const axios=require('axios');
const BASE_URL='https://api.upstox.com',CACHE_MS=20000,cache=new Map();
function token(){const t=process.env.UPSTOX_ACCESS_TOKEN;if(!t)throw new Error('UPSTOX_ACCESS_TOKEN is missing');return t;}
function num(v,f=0){const n=Number(v);return Number.isFinite(n)?n:f;} function pct(a,b){return b>0?((a-b)/b)*100:0;} function priceDir(v){return v>=0.10?'UP':v<=-0.10?'DOWN':'FLAT';} function oiDir(v){return v>=1?'UP':v<=-1?'DOWN':'FLAT';}
function classify(pc,oc){const p=priceDir(pc),o=oiDir(oc);if(p==='UP'&&o==='UP')return['LONG BUILDUP','BULLISH'];if(p==='DOWN'&&o==='UP')return['SHORT BUILDUP','BEARISH'];if(p==='UP'&&o==='DOWN')return['SHORT COVERING','BULLISH'];if(p==='DOWN'&&o==='DOWN')return['LONG UNWINDING','BEARISH'];return['NEUTRAL','NEUTRAL'];}
function expiryFromContracts(contracts){const today=new Date();today.setHours(0,0,0,0);return [...new Set((contracts||[]).map(c=>String(c?.expiry??c?.expiry_date??'').slice(0,10)).filter(x=>/^\d{4}-\d{2}-\d{2}$/.test(x)))].map(x=>({x,d:new Date(x+'T00:00:00')})).filter(x=>x.d>=today).sort((a,b)=>a.d-b.d)[0]?.x||null;}
async function getUnderlyingOIMood({instrumentKey,currentPrice,previousPrice}={}){const key=String(instrumentKey||'').trim();if(!key)return{mood:'UNKNOWN',sentiment:'UNKNOWN',dataAvailable:false,reason:'NO_UNDERLYING_KEY'};const cached=cache.get(key);if(cached&&Date.now()-cached.at<CACHE_MS)return{...cached.value,cached:true};try{const h={Accept:'application/json',Authorization:`Bearer ${token()}`};const contractsRes=await axios.get(`${BASE_URL}/v2/option/contract`,{params:{instrument_key:key},headers:h,timeout:12000});const contracts=Array.isArray(contractsRes?.data?.data)?contractsRes.data.data:[];const expiry=expiryFromContracts(contracts);if(!expiry)throw new Error('NO_CURRENT_OPTION_EXPIRY');const r=await axios.get(`${BASE_URL}/v2/option/chain`,{params:{instrument_key:key,expiry_date:expiry},headers:h,timeout:12000});const rows=Array.isArray(r?.data?.data)?r.data.data:[];if(!rows.length)throw new Error('EMPTY_OPTION_CHAIN');let callOI=0,putOI=0,prevCallOI=0,prevPutOI=0;for(const row of rows){const c=row?.call_options?.market_data||{},p=row?.put_options?.market_data||{};callOI+=Math.max(0,num(c.oi));putOI+=Math.max(0,num(p.oi));prevCallOI+=Math.max(0,num(c.prev_oi??c.previous_oi));prevPutOI+=Math.max(0,num(p.prev_oi??p.previous_oi));}const oi=callOI+putOI,previousOI=prevCallOI+prevPutOI,oiChange=oi-previousOI,oiChangePercent=pct(oi,previousOI),price=num(currentPrice),prevPrice=num(previousPrice);if(!(oi>0&&previousOI>0&&price>0&&prevPrice>0)){const value={mood:'UNKNOWN',sentiment:'UNKNOWN',dataAvailable:false,reason:'INCOMPLETE_OI_OR_PRICE',oi,previousOI,oiChange,oiChangePercent,callOI,putOI,prevCallOI,prevPutOI,source:`UPSTOX_OPTION_CHAIN_${expiry}`};cache.set(key,{at:Date.now(),value});return value;}const priceChange=price-prevPrice,priceChangePercent=pct(price,prevPrice),[mood,sentiment]=classify(priceChangePercent,oiChangePercent),value={mood,sentiment,dataAvailable:true,priceChange,priceChangePercent,oiChange,oiChangePercent,oi,previousOI,callOI,putOI,prevCallOI,prevPutOI,expiry,source:'UPSTOX_OPTION_CHAIN',reason:'OK'};cache.set(key,{at:Date.now(),value});return value;}catch(e){const value={mood:'UNKNOWN',sentiment:'UNKNOWN',dataAvailable:false,reason:`OI_API_FAILED:${e?.response?.status||e?.message||'UNKNOWN'}`};cache.set(key,{at:Date.now(),value});return value;}}
module.exports={getUnderlyingOIMood};